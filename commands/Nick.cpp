#include "../Servers.hpp"

int     Servers::Nick(Users &user, Parser &parser)
{
     std::cout << " -- [NICK] Command has been Chosen " << std::endl;
    (void) user;
    (void) parser;
    return (0);
}