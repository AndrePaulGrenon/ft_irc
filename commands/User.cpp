#include "../Servers.hpp"

int     Servers::User(Users &user, Parser &parser)
{
     std::cout << " -- [USER] Command has been Chosen " << std::endl;
    (void) user;
    (void) parser;
    return (0);
}